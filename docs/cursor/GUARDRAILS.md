# Жесткие правила для ИИ - GoblinHelperBot

## 🚫 ЧТО НЕЛЬЗЯ МЕНЯТЬ

### Критические файлы
- `index.js` - точка входа, инициализация бота
- `modules/sessions.js` - структура Redis сессий
- `settings.json` - конфигурация и константы
- `modules/util.js` - утилиты и хелперы

### Архитектурные принципы
- Паттерн `Composer.compose()` в модулях
- Структура `modules/{feature}/{actions|commands|scenes}/`
- Автоматический импорт через `getAllFilesFromFolder()`
- Redis как единственное хранилище данных

### Бизнес-логика
- Формула расчета свитков: `Math.floor(plus.length / 3) * 2 - scrollsSpent`
- Роли пользователей: `admin`, `adminPlus`, `rejected`
- Формат месяцев: `YYYY_MM`
- Специальные ID: EPINETOV, ALEKS, ANN, ARTYOM

### API контракты
- Структура `ctx.users.list[userId]`
- Структура `ctx.months.list[year][month]`
- Структура `ctx.globalSession.current`
- Функции `isAdmin()`, `isSuperUser()`, `getUserScrolls()`

## ✅ ЧТО МОЖНО МЕНЯТЬ

### Новые функции
- Добавление новых команд в существующие модули
- Создание новых модулей по паттерну
- Расширение существующих сцен
- Новые callback actions

### Улучшения
- Оптимизация производительности
- Улучшение UX/UI
- Добавление валидации
- Расширение логирования

### Рефакторинг
- Переименование переменных (с сохранением API)
- Улучшение читаемости кода
- Добавление комментариев
- Разделение больших функций

## 📝 ФОРМАТ ПАТЧЕЙ

### Структура коммита
```
feat: добавить команду /example
fix: исправить расчет билетиков
refactor: улучшить структуру модуля admin
docs: обновить документацию
```

### Описание изменений
```markdown
## Изменения
- Добавлена команда `/example` в модуль `common`
- Улучшена валидация входных данных
- Добавлено логирование ошибок

## Тестирование
- [ ] Команда работает в личных сообщениях
- [ ] Команда работает в группах
- [ ] Обработка ошибок корректна

## Совместимость
- ✅ Обратная совместимость сохранена
- ✅ API не изменен
- ✅ Структуры данных не изменены
```

### Код патчей
```javascript
// ✅ ХОРОШО - добавляем новую функцию
module.exports = Composer.command('example', async (ctx) => {
  // Новая логика
});

// ❌ ПЛОХО - изменяем существующую функцию
function getUserScrolls(ctx, userId) {
  // Изменение формулы расчета
  return Math.floor(plus.length / 2) * 3 - scrollsSpent; // НЕ ТРОГАТЬ!
}
```

## 🔍 PR ЧЕКЛИСТ

### Обязательные проверки
- [ ] Код следует существующим паттернам
- [ ] Не изменены критические файлы
- [ ] Сохранена обратная совместимость
- [ ] Добавлены тесты (если применимо)
- [ ] Обновлена документация

### Проверка архитектуры
- [ ] Модуль использует `Composer.compose()`
- [ ] Файлы в правильных папках
- [ ] Импорты корректны
- [ ] Нет циклических зависимостей

### Проверка данных
- [ ] Структуры данных не изменены
- [ ] Redis ключи не изменены
- [ ] API функции не изменены
- [ ] Константы не изменены

### Проверка безопасности
- [ ] Проверка прав доступа сохранена
- [ ] Валидация входных данных
- [ ] Обработка ошибок
- [ ] Логирование действий

## 🚨 КРИТИЧЕСКИЕ ОШИБКИ

### НЕ ДЕЛАТЬ
```javascript
// ❌ Изменение формулы билетиков
const scrolls = Math.floor(userData.purchases.groups.plus.length / 2) * 3;

// ❌ Изменение структуры пользователя
ctx.users.list[userId] = {
  // Новая структура
};

// ❌ Изменение ролей
const roles = ['newRole', 'anotherRole'];

// ❌ Изменение формата месяцев
const month = '2024-01'; // Должно быть '2024_01'
```

### ДЕЛАТЬ
```javascript
// ✅ Добавление новой функции
function calculateNewMetric(userData) {
  return userData.purchases.balance * 0.1;
}

// ✅ Расширение существующей функции
function getUserMessage(ctx, userData) {
  const baseMessage = getUserMessageBase(ctx, userData);
  return baseMessage + '\n\nНовая информация';
}

// ✅ Новый модуль
// modules/newfeature/index.js
const { Composer } = require('telegraf');
module.exports = Composer.compose([
  // Новые обработчики
]);
```

## 📋 ШАБЛОНЫ

### Новый модуль
```
modules/newfeature/
├── index.js
├── actions/
│   └── action1.js
├── commands/
│   └── command1.js
└── scenes/
    └── scene1.js
```

### Новая команда
```javascript
// modules/feature/commands/command.js
const { Composer } = require('telegraf');
const { isAdmin, log } = require('../../util');

module.exports = Composer.command('command', async (ctx) => {
  log(ctx);
  
  if (!isAdmin(ctx.from.id)) {
    ctx.reply('У вас нет прав');
    return;
  }
  
  // Логика команды
  ctx.reply('Результат');
});
```

### Новый action
```javascript
// modules/feature/actions/action.js
const { Composer } = require('telegraf');

module.exports = Composer.action('actionName', async (ctx) => {
  // Логика action
  ctx.answerCbQuery('Готово');
  ctx.editMessageText('Обновлено');
});
```

## 🎯 ПРИОРИТЕТЫ

1. **Сохранение работоспособности** - бот должен работать
2. **Совместимость** - существующие функции не ломаются
3. **Архитектура** - следование паттернам
4. **Безопасность** - проверка прав и валидация
5. **Производительность** - оптимизация при необходимости 