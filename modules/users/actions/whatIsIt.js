const { Composer, Markup } = require("telegraf");

module.exports = Composer.action('whatIsIt', async (ctx) => {
  try { await ctx.answerCbQuery(); } catch {}
  
  // Step 2: What it is explanation
  await ctx.editMessageText(
'🎯 <b>Что это за логово?</b>\n\n' +
'Это закрытое гоблинское сообщество для тех, кто ценит 3D-печать и редкие STL-сокровища.\n\n' +
'Здесь тебя ждёт:\n' +
'• Эксклюзивные модели, которых нет в свободном доступе\n' +
'• Доступ к кикстартерам и редким свиткам\n' +
'• Полные коллекции артефактов\n' +
'• Общение с друганами-гоблинами и мастерами печати\n\n' +
'Но! В логове есть правила. Хочешь их узнать?'
    {
      parse_mode: 'HTML',
      ...Markup.inlineKeyboard([
        [Markup.button.callback('📜 Какие правила?', 'showRules')]
      ])
    }
  );
});
